{
    "mcpServers": {
        "rtl-daemon": {
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/rtl-daemon-mcp.sh",
            "env": {
                "BIN_DIR": "${CLAUDE_PLUGIN_ROOT}/bin/"
            }
        }
    }
}
